#include <stddef.h>
#include <memory.h>
#include <iostream>
#include "LargeInt.c"
/** using namespace std;


enum
{

negative = -1;
zero = 0;
positive = 1;

}

class LargeInt
{
	
	char * num;
	size_t len;
	int sign;

}

public:
LargeInt();
LargeInt(const long long&);
LargeInt(const LargeInt&);
LargeInt(long long a);
~LargeInt();

friend int AbsoluteCompare(const LargeInt& a, const LargeInt& b);
friend int CompareWithSign();
friend LargeInt AbsolutePlus(const LargeInt& a, const LargeInt& b);

friend ostream& operator<<(ostream& out, const LargeInt& a);
ostream& operator<<(ostream& out, const LargeInt& a);

friend int AbsoluteCompare(const LongInt& a, const LongInt& b);

public:

friend bool operator<<(const LargeInt& a, const LargeInt& b);
friend bool operator>>(const LargeInt& a, const LargeInt& b);
friend bool operator>=(const LargeInt& a, const LargeInt& b);
friend bool operator<=(const LargeInt& a, const LargeInt& b);
friend bool operator==(const LargeInt& a, const LargeInt& b);
1 = (const LargeInt& a, const LargeInt& b);

if(CompareWithSign)(a,b) < 0) 
return true;
else return false;

**/
